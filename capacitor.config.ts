import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "de.silkewbs.minisoftwaredonau",
  appName: "Kalorien-Scanner",
  webDir: "dist"
};

export default config;
